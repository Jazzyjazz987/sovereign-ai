# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv*/
.venv*/
env/

# Env files with secrets
.env.local
.env.production

# Logs (keep directory, ignore files)
logs/*.log

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Secrets réels — ne jamais committer
.env

# Corpus RAG — documents internes CPA (procédures, mails, tickets), contiennent des
# données nominatives → JAMAIS dans git. Seul le README de structure est suivi.
corpus/*
!corpus/README.md
.venv-ft/

# Reranker fine-tuné — poids reproductibles via rag/finetune/train.py (GPU, ~1 min),
# trop volumineux pour git (model.safetensors ~1,1 Go).
rag/finetune/model/
